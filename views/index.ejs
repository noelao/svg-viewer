<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Penampil Ikon SVG (EJS)</title>
  <style>
    body { 
      font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
      background-color: #f4f4f9;
      color: #333;
      padding: 2rem; 
      margin: 0;
    }
    h1 {
      text-align: center;
      color: #444;
    }
    .gallery { 
      display: flex; 
      flex-wrap: wrap; 
      gap: 2rem; 
      justify-content: center;
      margin-top: 2rem;
    }
    .icon-container {
      background-color: #ffffff;
      border-radius: 8px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      padding: 1rem;
      text-align: center;
      transition: transform 0.2s ease-in-out;
      width: 150px;
    }
    .icon-container:hover {
      transform: translateY(-5px);
    }
    .icon-container img {
      display: block;
      margin: 0 auto 0.5rem;
      width: 100px; /* Lebar tetap */
      height: 100px; /* Tinggi tetap */
    }
    .icon-container p {
      margin: 0;
      font-size: 0.875rem;
      color: #666;
      word-break: break-all;
    }
    .no-icons {
        padding: 2rem;
        text-align: center;
        background-color: #fff3cd;
        border: 1px solid #ffeeba;
        color: #856404;
        border-radius: 8px;
    }
  </style>
</head>
<body>
  <h1>Daftar Ikon SVG</h1>

  <div class="gallery">
    <% if (svgFiles && svgFiles.length > 0) { %>
      <% svgFiles.forEach(file => { %>
        <div class="icon-container">
          <img src="/svg/icons/<%= file %>" alt="<%= file %>">
          <p><%= file %></p>
        </div>
      <% }); %>
    <% } else { %>
      <div class="no-icons">
        <p>Tidak ada file SVG yang ditemukan.</p>
      </div>
    <% } %>
  </div>

</body>
</html>